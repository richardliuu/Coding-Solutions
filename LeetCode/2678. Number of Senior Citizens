class Solution:
    def countSeniors(self, details: List[str]) -> int:
        amount = 0 

        for info in details:
            if int(info[11:13]) > 60:
                amount += 1

        return amount 
