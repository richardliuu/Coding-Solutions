class Solution:
    def countDigits(self, num: int) -> int:
        string = str(num)
        counts = 0 

        for i in string:
            if num % int(i) == 0:
                counts += 1 

        return counts
        
