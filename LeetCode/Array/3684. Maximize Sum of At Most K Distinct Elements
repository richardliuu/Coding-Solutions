class Solution:
    def maxKDistinct(self, nums: List[int], k: int) -> List[int]:
        distinct = set(nums)
        print(distinct)

        descending = sorted(list(distinct), reverse=True)


        return descending[:k]
    
        
