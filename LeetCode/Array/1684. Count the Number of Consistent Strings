class Solution:
    def countConsistentStrings(self, allowed: str, words: List[str]) -> int:
        good = set(allowed)
        num = 0

        for word in words:
            if all(char in good for char in word):
                num += 1
            
        return num 
        
