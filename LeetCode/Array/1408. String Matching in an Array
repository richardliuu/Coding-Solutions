class Solution:
    def stringMatching(self, words: List[str]) -> List[str]:
        res = set()

        for i in words:
            for j in words:
                if i != j and i in j:
                    res.add(i)

        return list(res)
        
