class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        max_num = 0

        for word in sentences:
            words = len(word.split())

            max_num = max(max_num, words)

        return max_num
        
