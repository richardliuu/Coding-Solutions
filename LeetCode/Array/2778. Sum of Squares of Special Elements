class Solution:
    def sumOfSquares(self, nums: List[int]) -> int:
        total = 0 
        length = len(nums)

        for i in range(len(nums)):
            if length % (i+1) == 0:
                total += nums[i]**2

        return total
