class Solution:
    def absDifference(self, nums: List[int], k: int) -> int:
        nums.sort()
        small_sum = sum(nums[:k])
        large_sum = sum(nums[-k:])
        return abs(small_sum - large_sum)
