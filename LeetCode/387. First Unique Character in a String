class Solution:
    def firstUniqChar(self, s: str) -> int:
        counts = Counter(s)
        unique = ""
        
        for element, count in counts.items():
            if count == 1:
                return s.index(element)

        return -1
