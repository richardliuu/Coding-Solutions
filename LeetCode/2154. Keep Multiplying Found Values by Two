class Solution:
    def findFinalValue(self, nums: List[int], original: int) -> int:
        if original in nums:
            while original in nums:
                original *= 2
                print(original)

            return original
        else:
            return original  
        
