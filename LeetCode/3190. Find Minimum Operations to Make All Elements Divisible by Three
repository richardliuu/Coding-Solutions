class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        operations = 0 

        for i in nums:
            operations += min(i % 3, 3 - i % 3)
        
        return operations
