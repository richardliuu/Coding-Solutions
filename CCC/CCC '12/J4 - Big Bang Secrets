import sys 

k = sys.stdin.readline()
k = int(k)
code = sys.stdin.readline().strip()
res = ""

for pos in range(len(code)):
    P = pos + 1 
    s = 3 * P + k

    # original letter 
    old = ord(code[pos]) - ord("A")
    new = (old - s) % 26

    res += chr(new + ord("A"))

print(res)

