import numpy as np

def cross_product(a, b):
    arr = [0] * len(a)

    arr[0] = (a[1] * b[2]) - (a[2] * b[1])
    arr[1] = (a[2] * b[0]) - (a[0] * b[2])
    arr[2] = (a[0] * b[1]) - (a[1] * b[0])

    return arr 
    
